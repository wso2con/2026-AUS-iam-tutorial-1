# Agent Notes

- Do not run project commands such as build, test, install, or dev server commands by default.
- The user prefers to run commands themselves and report back with results.
- Only run commands when the user explicitly asks for a specific command to be executed.
